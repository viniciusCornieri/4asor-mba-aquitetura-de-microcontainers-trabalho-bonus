image:
  repository: mysql
  tag: "8.0"
  pullPolicy: IfNotPresent

storage:
  size: 2Gi
  storageClass: local-path

resources:
  requests:
    memory: "512Mi"
    cpu: "500m"
  limits:
    memory: "1Gi"
    cpu: "1000m"

replicas: 2

mysql:
  rootPassword: ""
  user: wordpress
  password: ""
  database: wordpress